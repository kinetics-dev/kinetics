/**
 * Symbol for identifying custom roles metadata
 */
export const ROLES = Symbol("roles");

/**
 * Symbol for implementing kinetics as custom passport strategy
 */
export const KINETICS = Symbol("kinetics");
