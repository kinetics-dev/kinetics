/**
 * NOTE: This is an example only
 *
 * Roles are unique and customizable to each workspace.
 * Use anything you like for enum keys and the role id for enum values
 *
 * @see {@link https://app.kinetics.dev/settings}
 *
 */
export enum Role {
  Admin = "admin",
}
